import { Divider } from 'antd'
import styled from 'styled-components'
import { colors } from '../../constants/colors'

export const StyledDivider = styled(Divider)`
  border: solid 0.1px ${colors.oldSilver};
`
